use flake

# Sign commits/tags from the moment a contributor enters the directory —
# surfacing the signing requirement at commit time rather than at PR push.
# (Mirrors midnightntwrk/midnight-did's .envrc convention.)
git config --local commit.gpgsign true
git config --local tag.gpgsign true
